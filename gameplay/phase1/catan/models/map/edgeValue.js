var catan = catan || {};
catan.models = catan.models || {};
catan.models.map = catan.models.map || {};

catan.models.map.EdgeValue = (function() {

    // Constructor 
    function EdgeValue(json) {

    }

    EdgeValue.prototype.example = function() {
        return null;
    };

    return EdgeValue;
})();