var catan = catan || {};
catan.models = catan.models || {};
catan.models.map = catan.models.map || {};

catan.models.map.HexGrid = (function() {

    // Constructor 
    function HexGrid(json) {

    }

    HexGrid.prototype.example = function() {
        return null;
    };

    return HexGrid;
})();