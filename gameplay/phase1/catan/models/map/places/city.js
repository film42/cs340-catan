var catan = catan || {};
catan.models = catan.models || {};
catan.models.map = catan.models.map || {};
catan.models.map.places = catan.models.map.places || {};

catan.models.map.places.City = (function() {

    // Constructor 
    function City(json) {

    }

    City.prototype.example = function() {
        return null;
    };

    return City;
})();